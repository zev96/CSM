"""Only :mod:`csm_core.assembler.cards` remains — the article assembler
itself was retired together with the 创作区 page. ``cards.py`` stays because
``routes/vault.py`` (card_coverage / card_sections, used by the template
BlockEditor) needs ``note_sections`` / ``find_card_section``.
"""
