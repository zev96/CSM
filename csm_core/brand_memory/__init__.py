"""Brand/model identity helpers — see :mod:`csm_core.brand_memory.identity`.

品牌型号记忆库（model/resolver/whitelist 等）已随素材库页面下线；只剩
``identity.py`` 供模板库的竞品卡覆盖度检查（routes/vault.py card_coverage）
与 ``csm_core.assembler.cards`` 使用。
"""
