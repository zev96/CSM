"""Framework-specific tests — will be deleted in Task 10.

compose_draft_framed was removed from csm_core.assembler.render in Task 7.
These tests are skipped until the framework layer cleanup in Task 10.
"""
import pytest

pytest.skip("framework layer removed from pipeline (Task 7); cleanup in Task 10", allow_module_level=True)
